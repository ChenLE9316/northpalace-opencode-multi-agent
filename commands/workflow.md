---
description: Start or continue a bounded Build workflow under the L1-L3 collaboration contract.
agent: build
subtask: false
---

Start or continue `$ARGUMENTS` with a stable workflow id matching `[A-Za-z0-9][A-Za-z0-9-]{0,63}`. Enforce one mutating Build root, risk-adaptive routing, explicit acceptance criteria, dependencies, and one writer per owned path under `@rules/orchestration.md`.

Use direct L2 specialists for common work and one of the five approved coordinators only for bounded L3 leaf tasks. Reuse a task id only for the same valid agent session; relay sibling evidence through the parent, use fresh review/security sessions, and persist a handoff only on the contract triggers.
