---
description: Start or continue a bounded Build workflow under the runtime-aware L1-L3 governance contract.
agent: build
subtask: false
---

Start or continue `$ARGUMENTS` with a stable workflow id matching `[A-Za-z0-9][A-Za-z0-9-]{0,63}`. Enforce one mutating Build root, risk-adaptive routing, explicit acceptance criteria, dependencies, and one writer per owned path under `@rules/orchestration.md`.

Use direct L2 specialists for common work and an approved coordinator only for bounded L3 leaves. Parallel writers require both path disjointness and semantic independence; interface/schema/lock/generated dependencies must be ordered.

Reuse task id only for the same valid agent/session when prior context materially helps. Relay sibling evidence through the parent, use fresh review/security sessions, and checkpoint registry state before compaction/steps/large-fanout reconstruction becomes fragile.

For shell-capable writers, declare expected generated/lock/artifact effects and inspect status/diff after mutation. Unexpected source changes block for L1 reconciliation. Cancellation never implies rollback.

Completion requires all writers settled → stable final snapshot → authoritative final verification → fresh review/security as required. Do not attempt alternate shell syntax to bypass a hard-denied external effect.
