---
description: Execute a bounded implementation through delivery ownership
agent: northpace
subtask: true
---

Implement `$ARGUMENTS` through `delivery-lead`. Keep write scopes explicit, avoid conflicting parallel mutations, preserve reversibility, and require artifact-level verification before completion.
